# 開発時の注意事項

このファイルは、tonari-town で作業する開発者・AIエージェント向けの共有ルールです。
公開リポジトリに含まれるため、秘密情報や個人用の環境情報は記載しません。

## プロジェクトの範囲

- AIパートナーの台詞が家から見える街を、三人称視点で散歩するWebアプリです。
- 台詞はユーザーが外部のAIで生成し、JSONを貼り付けて登録します。
- 街データを共有します。ユーザー間チャットや移動のリアルタイム同期は現時点の範囲外です。
- 想定構成と実装済みの機能を区別し、変更時は README.md も更新してください。

## Gitと公開範囲

- GitHubリポジトリは `enashimaga/tonari-town` です。
- GitHubへの作成・更新操作前に、実際の操作アカウントが `enashimaga` であることを確認してください。
- コミット名義は `Enashimaga`、メールはこのリポジトリに設定されたGitHubの `noreply` アドレスを使います。
- 個人用メールや別名義が混入しないよう、コミット前に有効なローカル設定を確認してください。グローバル設定は変更しません。
- コミット前にステージした差分を確認し、意図しないファイルや秘密情報が含まれていないことを確認してください。

## 秘密情報

- 実際の認証情報は `.env.local` などのGit管理対象外ファイル、またはサービス側のシークレット設定に保存します。
- `.env.example` は変数名と明らかなプレースホルダーのみとし、実値をコピーしません。
- XのClient Secret、アクセストークン、Supabaseの管理用キー、秘密鍵をソース・ログ・スクリーンショット・Issue・PRに載せません。
- 秘密情報を含むファイルや環境変数を、確認のために丸ごと出力しません。必要な設定の有無だけを確認します。
- ブラウザへ埋め込まれる環境変数やビルド成果物は公開情報として扱います。秘密情報を含めません。
- `.gitignore` だけで安全と判断せず、追跡済みファイルと公開する差分も確認します。
- 漏えいが判明した場合は公開作業を止め、値を再掲せずに報告します。ファイルの削除だけでは対処完了とせず、失効・再発行を優先します。

## 認証・データアクセス

- XログインにはSupabase Authを利用する想定です。Xのパスワードはアプリで受け取りません。
- Xの権限はログインに必要な範囲に限定し、投稿・DMなどの権限を機能上の理由なく追加しません。
- 所有者IDは検証済みの認証情報から決定し、ユーザー入力の所有者IDを信用しません。
- 公開データと認証情報・非公開データを分け、公開済みの家と台詞だけを閲覧対象にします。
- クライアントからアクセスするテーブルにはRLSと適切な権限を設定し、編集・削除は所有者に限定します。
- 区画の重複取得はデータベースの制約などで防ぎ、画面側の空き確認だけに依存しません。
- X認証の設定方法、要求権限、料金は実装時の公式情報と実際の管理画面で確認し、ログインが無料と決めつけません。

## JSONと街の表示

- 貼り付けたJSONは信頼できない入力として扱い、構造・バージョン・文字数・件数・全体サイズを検証します。
- 保存時にもサーバー側またはデータベース側で制約を適用します。
- 台詞はテキストとして表示し、HTMLやスクリプトとして実行しません。
- 公開前に、ユーザーが取り込んだ台詞を確認できるようにします。
- 街は地区単位で取得し、移動や吹き出し切り替えのたびに通信しない設計を基本にします。

## コードコメント

- コードを読めば分かる処理内容や、型・引数・戻り値の言い換えをコメントに残しません。JSDocなども同じ方針です。
- 説明が必要な場合は、まず命名や処理の構造で意図が伝わるようにします。
- コメントは、判断の理由、外部仕様の制約、回避策の背景など、コードだけでは分からない情報に絞ります。参照先や回避策を外せる条件があれば添えます。
- 実装を変更した際は関連するコメントも確認し、古くなった説明や不要になったコメントは更新・削除します。

## 変更と検証

- 変更に応じたビルド・型チェック・テストを実行し、実行していない確認を成功と報告しません。
- 認証・保存機能では、未ログインや別ユーザーによる不正な更新が拒否されることも確認します。
- 実ユーザーの台詞や認証情報をテストデータに使わず、架空のデータを使います。
- 既存データの削除、破壊的な移行、有料サービスの契約は、既存のユーザー承認の範囲を確認してから行います。
